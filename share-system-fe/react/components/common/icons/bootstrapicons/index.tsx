import React from 'react';

type Props = React.HTMLAttributes<HTMLDivElement> & {}

const BootstrapIconComponent: React.FC<Props> = (props): React.ReactNode => {
    return (
        <React.Fragment>

        </React.Fragment>
    )
};

export default BootstrapIconComponent;